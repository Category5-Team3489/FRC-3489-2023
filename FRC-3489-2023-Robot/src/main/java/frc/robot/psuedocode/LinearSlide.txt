Linear Slide
    neo
    2 limit switch

Set Linear Slide
    when button is pressed
    set linear slide to position (fully extended, half, retracted)
    set LEDs

    extended
        execute
            linearSlide.extend
        is finished
            if limit switch = true
        end
            stop motor
            set Led

    retract
        execute
            linearSlide.retract
        isFinished
            if limit switch = true
        end
            stop motor 
            set LEDs
